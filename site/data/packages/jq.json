{
    "name": "jq",
    "version": "1.7.1",
    "description": "Lightweight command-line JSON processor",
    "category": "util/cli",
    "source": "https://github.com/jqlang/jq (jq-1.7.1)",
    "license": "MIT",
    "size": 141538,
    "sha256": "9e1060219f0fcc203aa20f607199cffccf13d92bcdda292b497ef8a6eb9f8724",
    "download": "/packages/jq-1.7.1.lha",
    "requires": [],
    "aminet": "",
    "stack": 131072,
    "status": "stable",
    "revision": 1,
    "published_at": "2026-03-23T17:05:00Z",
    "test_count": 50,
    "test_pass": 50,
    "porting_notes": "First 68k AmigaOS port of jq. A PPC-MorphOS port exists on Aminet (v1.8.1, Oct 2025) but is incompatible with classic 68k hardware.\n\nBuilt with -O0 due to a bebbo-gcc 6.5.0 codegen bug (crash-patterns #16) that corrupts 16-byte struct returns at -O1/-O2. Binary is ~12% larger than an optimised build.\n\nBuilt without oniguruma: regex builtins (test, match, capture, scan, sub, gsub) return runtime error messages. All other jq functionality works.\n\nBuilt without decNumber: uses standard IEEE 754 doubles for JSON number arithmetic. Uses -std=gnu99 (ADR-022) for jq's 60+ C99 for-init declarations.\n\n128KB stack cookie. pthread TLS replaced with static singleton stubs (single-threaded AmigaOS). fdopen()+open() replaced with fopen()+stat() (crash-patterns #12). Large local buffer in jv_file.c made static (crash-patterns #10).\n\nReviewed by memory-checker (14 leak paths fixed across 2 rounds), perf-optimizer (output buffering, MurmurHash alignment, escape batching applied), hardware-expert (stack and alignment verified for 68000-68060), and profiler (87% startup cost confirmed, linear scaling).\n\n50/50 FS-UAE tests pass. Test suite covers all 25+ flags, error paths (RC 10/20), edge cases, Amiga-specific path handling, and real-world JSON processing scenarios.",
    "known_limitations": "Regex builtins unavailable: test(), match(), capture(), scan(), sub(), gsub() return runtime errors (built without oniguruma). No arbitrary-precision decimals: uses IEEE 754 doubles (1.1 + 2.2 != 3.3). GNU math extensions (j0, j1, yn, tgamma, cbrt, erf) return runtime errors. Slow startup (~1s on vamos for builtin compilation, ~7-12s on stock A1200) — architectural to jq, not fixable at source level. Practical for config file processing on accelerated A1200 or better.",
    "readme": "Short:        Lightweight command-line JSON processor\nType:         util/cli\nArchitecture: m68k-amigaos >= 3.0\nUploader:     duncan (at) bowring (dot) us\nAuthor:       Stephen Dolan / jqlang (ported by Duncan Bowring)\nVersion:      1.7.1\n\njq - Lightweight command-line JSON processor\n\njq is like sed for JSON data. You can slice, filter, map, and\ntransform structured data with the same ease that sed, awk, grep,\nand friends let you manipulate text.\n\nOriginal author: Stephen Dolan / jqlang contributors\nPorted by Duncan Bowring using amiport (AI-assisted porting toolkit).\nProject: https://github.com/bdgscotland/amiport\n\nCross-compiled with m68k-amigaos-gcc for AmigaOS 3.x (68020+).\nTested in vamos emulator and FS-UAE.\n\nUsage:\n  jq [options] <filter> [file ...]\n  echo '{\"name\":\"Amiga\"}' | jq '.name'\n  jq '.[] | select(.age > 30)' data.json\n  jq -r '.results[].title' api_response.json\n\nBuilt without oniguruma regex library. Regex builtins (test,\nmatch, capture) return a runtime error message. All other jq\nfunctionality works: select, map, keys, has, add, reduce,\ngroup_by, sort_by, unique, flatten, range, path, env, debug,\n@base64, @uri, @csv, @tsv, @html, tojson/fromjson, and more.\n\nIncludes full source code (original POSIX and ported Amiga versions)\nand PORT.md documenting every transformation applied."
}
