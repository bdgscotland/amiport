{
    "name": "jq",
    "version": "1.7.1-2",
    "description": "Lightweight command-line JSON processor",
    "category": "util/cli",
    "source": "https://github.com/jqlang/jq (jq-1.7.1)",
    "license": "MIT + Oniguruma BSD",
    "size": 141538,
    "sha256": "9e1060219f0fcc203aa20f607199cffccf13d92bcdda292b497ef8a6eb9f8724",
    "download": "/packages/jq-1.7.1.lha",
    "requires": [],
    "aminet": "",
    "stack": 131072,
    "status": "stable",
    "revision": 2,
    "published_at": "2026-03-23T17:05:00Z",
    "test_count": 60,
    "test_pass": 60,
    "porting_notes": "First 68k AmigaOS port of jq. A PPC-MorphOS port exists on Aminet (v1.8.1, Oct 2025) but is incompatible with classic 68k hardware.\n\nBuilt with -O0 due to a bebbo-gcc 6.5.0 codegen bug (crash-patterns #16) that corrupts 16-byte struct returns at -O1/-O2.\n\nRevision 2: Includes Oniguruma 6.9.9 regex engine (ASCII-only build, 227 KB). All regex builtins work: test, match, capture, scan, sub, gsub. Named capture groups supported. Single-entry regex compilation cache for performance. Memory leak on regex error path fixed.\n\nBuilt without decNumber: uses standard IEEE 754 doubles for JSON number arithmetic. Uses -std=gnu99 (ADR-022) for jq's 60+ C99 for-init declarations.\n\n128KB stack cookie. pthread TLS replaced with static singleton stubs (single-threaded AmigaOS). fdopen()+open() replaced with fopen()+stat() (crash-patterns #12). Large local buffer in jv_file.c made static (crash-patterns #10).\n\nReviewed by memory-checker (15 leak paths fixed across 3 rounds), perf-optimizer (output buffering, MurmurHash alignment, escape batching, regex cache applied), hardware-expert (stack and alignment verified for 68000-68060), and profiler (87% startup cost confirmed, linear scaling).\n\n60/60 FS-UAE tests pass (50 original + 10 regex).",
    "known_limitations": "ASCII-only regex: Unicode character properties (\\p{Letter}) not supported. All other regex features work. No arbitrary-precision decimals: uses IEEE 754 doubles (1.1 + 2.2 != 3.3). GNU math extensions (j0, j1, yn, tgamma, cbrt, erf) return runtime errors. Slow startup (~1s on vamos for builtin compilation, ~7-12s on stock A1200) -- architectural to jq, not fixable at source level. Practical for config file processing on accelerated A1200 or better.",
    "readme": "Short:        Lightweight command-line JSON processor\nType:         util/cli\nArchitecture: m68k-amigaos >= 3.0\nUploader:     duncan (at) bowring (dot) us\nAuthor:       Stephen Dolan / jqlang (ported by Duncan Bowring)\nVersion:      1.7.1-2\n\njq - Lightweight command-line JSON processor\n\njq is like sed for JSON data. You can slice, filter, map, and\ntransform structured data with the same ease that sed, awk, grep,\nand friends let you manipulate text.\n\nIncludes Oniguruma 6.9.9 regex engine (ASCII-only build).\nAll regex builtins work: test, match, capture, scan, sub, gsub.\nNamed capture groups supported (Perl NG syntax).\n\nAll jq functionality works: select, map, keys, has, add,\nreduce, group_by, sort_by, unique, flatten, range, path, env,\ndebug, @base64, @uri, @csv, @tsv, @html, tojson/fromjson."
}
