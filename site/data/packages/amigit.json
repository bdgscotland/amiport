{
    "name": "amigit",
    "version": "0.1",
    "revision": 6,
    "description": "Local git CLI for AmigaOS 3.x (preview)",
    "category": "dev/misc",
    "source": "amiport-native (libgit2 1.8.5)",
    "license": "GPL-2.0 (libgit2) + MIT (amiport code)",
    "size": 521236,
    "sha256": "6959939981203b46df4adc112628134c63e23c0eb9eb29622471cc982a8720f6",
    "machine_size": 449646,
    "machine_sha256": "ad43fe32a5b6816ed3199fe185f047dc5e609c68a8730fecf558e7aed6c997a7",
    "download": "/packages/amigit-0.1-6.lha",
    "requires": [],
    "stack": 262144,
    "status": "preview",
    "readme": "Short:        Local git CLI for AmigaOS 3.x (preview)\nType:         dev/misc\nArchitecture: m68k-amigaos >= 68020\nUploader:     duncan@bowring.us\nAuthor:       Duncan Bowring (amiport, built on libgit2)\nVersion:      0.1-6\nURL:          https://github.com/bdgscotland/amiport\n\namigit 0.1-6 -- DEVELOPER PREVIEW\n\nThis is an early developer preview, NOT a stable git client.\nSuitable for early testers and feedback. Not yet suitable for\nmanaging real projects.\n\nWhat works today:\n  - Local repository operations: status, log, show, diff, add,\n    commit, branch, checkout, tag\n  - Reading existing repos created by another git\n  - Single-letter assigned-volume init (T:foo, R:foo after\n    AmigaDOS Assign)\n\nWhat does NOT work yet:\n  - amigit init from a multi-character volume name (WORK:,\n    Ram Disk:, System 3.1:). You have to AmigaDOS Assign a\n    single-letter alias first. See \"Init friction\" below.\n  - Network: no clone, no fetch, no push, no remote, no pull.\n    The whole collaboration value of git is missing. amigit\n    currently only manages LOCAL repositories.\n  - Multi-word commit messages via -m (AmigaDOS splits argv on\n    whitespace). Use -F <file> for multi-word messages instead\n    (added 0.1-5): write your message to a file and pass it via\n    -F. Multi-line messages work too.\n  - Annotated tags with messages (tag -a). Only lightweight\n    tags work today. Planned for a future revision.\n  - Merge, rebase, cherry-pick, revert, stash. None implemented.\n  - Color output, pager integration, interactive flows.\n\nThe init friction:\n  Out of the box, \"amigit init\" from a typical Shell prompt\n  (1.WORK:>, 1.Ram Disk:>) does NOT work. The libgit2 library\n  this port embeds only recognizes single-character drive\n  prefixes (X:) as path roots. AmigaOS multi-character volume\n  names confuse it. amigit detects the case at startup and\n  emits a friendly error pointing at the workaround.\n\n  Workaround:\n    1.WORK:> Assign R: WORK:\n    1.WORK:> R:\n    1.R:> amigit init R:myproject\n\n  We are working on a real fix. See PORT.md inside the LHA for\n  the engineering notes on three approaches that were tried in\n  the 0.1.x line and rolled back.\n\nRoadmap:\n  0.1-6 (this release) -- bundled libgit2 / zlib / posix-shim\n        rebuilt at -m68020 alongside the amigit binary itself.\n        0.1-5 was a -m68020 binary linked against -m68000\n        libraries, so the hot work (libgit2 pack/diff, zlib\n        inflate, POSIX shims) was still running 68000 code.\n        0.1-6 fixes that: every code path in the final image\n        uses the 68020 instruction set. Noticeable on diff,\n        log, and status over larger histories.\n  0.2 -- init works from any volume + annotated tag messages\n        (tag -a -F <file>). Removes the init friction wall\n        above. Target: weeks, not months.\n  1.0 -- HTTPS clone, fetch, log, diff against public repos\n        via a libgit2 smart-HTTP backend on AmiSSL. Target:\n        roughly 6-10 weeks of focused work. This is the\n        watershed release -- the moment amigit becomes a tool\n        you would actually use to explore real codebases on\n        AmigaOS.\n  Future -- HTTPS push, libssh2 SSH support, merge / rebase /\n           stash. Probably another 4-8 weeks beyond 1.0.\n\nWhy amigit is local-only today:\n  Upstream git is structurally infeasible on AmigaOS because\n  its command dispatch is built on fork() / execvp(), which\n  AmigaOS does not provide. amigit is built from scratch on\n  libgit2 1.8.5 (pruned to remove networking, submodule\n  cloning, and the failalloc harness) which gives us a clean\n  embeddable git core but no network transport. Networking\n  is a large, separate engineering effort tracked in the\n  roadmap above.\n\nWhat this preview is good for:\n  - Validating the libgit2 + amiport posix-shim stack on real\n    AmigaOS hardware (your A2000+Vampire, A1200, A3000 / 4000)\n  - Walking the history of an existing .git/ directory copied\n    over from a Linux machine\n  - Experimenting with a journal-style local-only workflow\n  - Reporting bugs that help shape 0.2 / 0.3 / 1.0\n\nWhat this preview is NOT good for:\n  - Daily git workflow on real projects (yet)\n  - Collaboration via GitHub / GitLab / etc (no networking yet)\n  - Anything that requires merge, rebase, or stash\n\nProject: https://github.com/bdgscotland/amiport\nBuilt on: libgit2 1.8.5, zlib 1.3.1, amiport posix-shim\n\nCross-compiled with m68k-amigaos-gcc 6.5.0b for AmigaOS 3.x.\nTargets 68020 minimum (no plain 68000 / A500 support --\naccelerated Amigas only). Tested in FS-UAE A1200 with 87/87\nfunctional tests passing -- but those tests cover synthetic\nhappy paths and do not yet exercise the full real-world\nworkflow. Real workflow validation is a 0.2 milestone.\n\nBinary: ~1.03 MB (stripped). Stack: 256 KB. Requires 4 MB\nfree RAM at runtime. 68020 minimum.\n\nBug reports and feedback welcome via the GitHub issue tracker:\nhttps://github.com/bdgscotland/amiport/issues\n\nExit codes (Amiga-native):\n  0  = success (RETURN_OK)\n  10 = any error (RETURN_ERROR)\n  20 = fatal (RETURN_FAIL)\n\nEnvironment variables (for commit author/committer identity):\n  GIT_AUTHOR_NAME         (default: amigit user)\n  GIT_AUTHOR_EMAIL        (default: amigit@localhost)\n  GIT_COMMITTER_NAME      (default: amigit user)\n  GIT_COMMITTER_EMAIL     (default: amigit@localhost)\n",
    "published_at": "2026-04-14T00:15:00Z",
    "updated_at": "2026-04-14T13:30:00Z",
    "porting_notes": "DEVELOPER PREVIEW (0.1.x line). First git client implementation effort for AmigaOS 3.x. Written from scratch as an amiport-native CLI on top of the pruned lib/libgit2/libgit2.a (1.8.5) because upstream git's fork()/execvp()-based command dispatch is structurally infeasible on 68k AmigaOS. 0.1-5 added commit -F for multi-word messages and promoted amigit port TUs to -O1. 0.1-6 closes Track A: bundled libgit2, zlib, and posix-shim are now built at -m68020 alongside the binary itself, so the hot code paths (libgit2 pack/diff/xdiff, zlib inflate, POSIX shims) finally use the 68020 instruction set instead of running 68000 code inside a 68020 process (which is what 0.1-5 accidentally shipped -- cosmetic -m68020 on a 68000 library backend). The 000 library variants (libz.a, libgit2.a, libamiport.a) remain the default and are still what every other amiport port links against; the 020 variants (libz-020.a, libgit2-020.a, libamiport-020.a) are opt-in and today only amigit consumes them. Three honest gaps still hold at 0.1-6: (1) 'amigit init' from a typical Shell prompt on a multi-char volume name doesn't work without an AmigaDOS Assign workaround (libgit2 git_fs_path_root limitation, three real-fix attempts tried and rolled back -- see PORT.md), (2) no networking yet (clone/fetch/push need a custom libgit2 smart-HTTP backend on AmiSSL, ~6-10 weeks of focused work tracked as the 1.0 milestone), (3) the 87-test suite covers synthetic happy paths rather than real-world workflows. Roadmap: 0.2 = init works from any volume + annotated tag messages; 1.0 = HTTPS clone/fetch/log against public repos. Bug reports welcome.",
    "test_count": 87,
    "test_pass": 87,
    "known_limitations": "DEVELOPER PREVIEW -- not yet stable for daily use. (1) No network commands at all -- clone/fetch/push/pull/remote/submodule require bsdsocket + libssh2 or http-shim in a future Phase 4 (1.0 milestone). The collaboration value of git is missing. (2) 'amigit init' from a multi-character volume name ('WORK:', 'Ram Disk:', 'System 3.1:') produces a friendly error pointing at the AmigaDOS Assign workaround -- libgit2's git_fs_path_root only recognizes single-character ASCII drive prefixes. Workaround: 'Assign R: WORK: ; amigit init R:foo'. Real fix deferred. (3) The 87-test suite covers synthetic happy paths; full real-world workflow validation is a 0.2 milestone. (4) No merge, rebase, cherry-pick, revert, or stash. (5) No interactive flows (rebase -i, add -i, commit --amend with editor). (6) Commit messages via -m must be single shell tokens; use -F <file> for multi-word messages (added 0.1-5). (7) No color output, no pager integration. (8) Empty initial commits rejected. (9) Requires 4 MB free RAM at runtime. (10) 68020 minimum -- no plain 68000/A500 support."
}
