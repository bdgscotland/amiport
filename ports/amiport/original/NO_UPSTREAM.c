/*
 * NO_UPSTREAM.c -- Placeholder file.
 *
 * amiport is amiport-native code, not a port of upstream software.
 * It is the package manager client for AmigaOS 3.x, written from
 * scratch on top of lib/http-shim, lib/bsdsocket-shim, and
 * lib/posix-shim. There is no upstream project to preserve here.
 *
 * See ports/amiport/PORT.md for the implementation history.
 *
 * This file exists only to satisfy the port-directory hygiene rule
 * that every port has an original/ directory with at least one .c
 * file. It is never compiled (see Makefile SOURCES list) and is not
 * part of the amiport binary.
 */

/* Intentionally empty -- see block comment above. */
