# FS-UAE Test Report: tee

## Summary

| Field | Value |
|-------|-------|
| Port | tee |
| Date | 2026-03-22 01:01:11 |
| Duration | 63s |
| Platform | FS-UAE 3.2.35 (A1200, Kickstart 3.1) |
| Binary | `WORK:tee` (30K) |
| Test method | ARexx harness → TAP output |
| Result | **PASS** — 11/11 passed |

## Test Results

```
1..11
ok 1 - basic stdin passthrough to stdout (no output files)
ok 2 - write stdin to a single output file while passing through to stdout
ok 3 - append mode (-a) appends to existing file instead of overwriting
ok 4 - write to multiple output files simultaneously
ok 5 - -i flag (ignore SIGINT) is accepted without error
ok 6 - invalid flag prints usage to stderr and returns RETURN_ERROR
ok 7 - nonexistent output directory causes open failure and returns RETURN_ERROR
ok 8 - empty input produces no stdout output and returns RETURN_OK
ok 9 - long line (>1024 chars) passes through 64KB buffer without truncation
ok 10 - Amiga RAM: volume path accepted as output file (Amiga path handling)
ok 11 - Amiga T: volume path accepted as output file (temp volume path handling)
# passed: 11 failed: 0 total: 11
```

### Breakdown

| # | Test | Status | Details |
|---|------|--------|---------|
| 1 | basic stdin passthrough to stdout (no output files) | PASS | |
| 2 | write stdin to a single output file while passing through to stdout | PASS | |
| 3 | append mode (-a) appends to existing file instead of overwriting | PASS | |
| 4 | write to multiple output files simultaneously | PASS | |
| 5 | -i flag (ignore SIGINT) is accepted without error | PASS | |
| 6 | invalid flag prints usage to stderr and returns RETURN_ERROR | PASS | |
| 7 | nonexistent output directory causes open failure and returns RETURN_ERROR | PASS | |
| 8 | empty input produces no stdout output and returns RETURN_OK | PASS | |
| 9 | long line (>1024 chars) passes through 64KB buffer without truncation | PASS | |
| 10 | Amiga RAM: volume path accepted as output file (Amiga path handling) | PASS | |
| 11 | Amiga T: volume path accepted as output file (temp volume path handling) | PASS | |

## Environment

| Component | Version/Path |
|-----------|-------------|
| FS-UAE | 3.2.35 |
| Kickstart | 3.1 (40.68) |
| Amiga model | A1200 (68020) |
| Compiler | m68k-amigaos-gcc (bebbo) |
| POSIX shim | libamiport.a |
| Regex emu | libamiport-emu.a |
| Test harness | ARexx (test-runner.rexx) |

## Test Cases

Each test runs the command inside AmigaOS, captures stdout to a file,
and compares against the expected output string.

```
# FS-UAE test suite for tee 1.15 (OpenBSD)
# Category 1 (CLI tool) -- minimum 8 tests required
# Generated by test-designer agent
#
# NOTE: tee reads from STDIN_FILENO only -- no file input argument.
# All tests that need stdin input use WORK:test-tee-run.rexx, an ARexx
# wrapper that creates an Execute script with < redirection internally.
# This avoids prohibited < and | in CMD lines (ARexx ADDRESS COMMAND
# limitation) while still feeding data to tee.

TEST: basic stdin passthrough to stdout (no output files)
CMD: rx WORK:test-tee-run.rexx WORK:test-tee-input.txt
EXPECT: tee test line one
EXPECT_RC: 0

TEST: write stdin to a single output file while passing through to stdout
CMD: rx WORK:test-tee-run.rexx WORK:test-tee-input.txt T:tee-single-out.txt
EXPECT: tee test line one
EXPECT_RC: 0

TEST: append mode (-a) appends to existing file instead of overwriting
CMD: rx WORK:test-tee-run.rexx WORK:test-tee-input.txt -a T:tee-append-out.txt
EXPECT: tee test line one
EXPECT_RC: 0

TEST: write to multiple output files simultaneously
CMD: rx WORK:test-tee-run.rexx WORK:test-tee-input.txt T:tee-multi1.txt T:tee-multi2.txt
EXPECT: tee test line one
EXPECT_RC: 0

TEST: -i flag (ignore SIGINT) is accepted without error
CMD: rx WORK:test-tee-run.rexx WORK:test-empty.txt -i
EXPECT_RC: 0

TEST: invalid flag prints usage to stderr and returns RETURN_ERROR
CMD: WORK:tee -z
EXPECT_RC: 10

TEST: nonexistent output directory causes open failure and returns RETURN_ERROR
CMD: rx WORK:test-tee-run.rexx WORK:test-tee-input.txt WORK:nodir/nosuchfile.txt
EXPECT_RC: 10

TEST: empty input produces no stdout output and returns RETURN_OK
CMD: rx WORK:test-tee-run.rexx WORK:test-empty.txt
EXPECT:
EXPECT_RC: 0

TEST: long line (>1024 chars) passes through 64KB buffer without truncation
CMD: rx WORK:test-tee-run.rexx WORK:test-longline.txt
EXPECT_CONTAINS: MARKER
EXPECT_RC: 0

TEST: Amiga RAM: volume path accepted as output file (Amiga path handling)
CMD: rx WORK:test-tee-run.rexx WORK:test-tee-input.txt RAM:tee-amiga-test.txt
EXPECT: tee test line one
EXPECT_RC: 0

TEST: Amiga T: volume path accepted as output file (temp volume path handling)
CMD: rx WORK:test-tee-run.rexx WORK:test-oneline.txt T:tee-temp-test.txt
EXPECT: hello world
EXPECT_RC: 0
```

## Emulator Log

```
(log not captured in this run)
```

## Sentinel File

Written by the ARexx harness when all tests complete:

```
TESTS_COMPLETE
passed=11
failed=0
total=11
```

---
Generated by `make test-fsemu TARGET=ports/tee`
Report template: `toolchain/templates/test-report.md.template`
