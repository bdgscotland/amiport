# Makefile for tsort (amiport)
TARGET      = tsort
SOURCES     = ported/tsort.c
VERSION     = 1.38
REVISION    = 1
DESCRIPTION = Topological sort of a directed graph
AUTHOR      = Marc Espie, OpenBSD

AMINET_CAT  = util/cli

include ../common.mk

.PHONY: test

test: $(TARGET)
	@echo "=== Testing $(TARGET) ==="
	@echo "=== $(TARGET) tests complete ==="
