# Makefile for join (amiport)

TARGET      = join
SOURCES     = ported/join.c
VERSION     = 1.34
REVISION    = 1
DESCRIPTION = Relational join on sorted files
AUTHOR      = OpenBSD

AMINET_CAT  = util/cli

include ../common.mk

.PHONY: test

test: $(TARGET)
	@echo "=== Testing $(TARGET) ==="
	@echo "=== $(TARGET) tests complete ==="
