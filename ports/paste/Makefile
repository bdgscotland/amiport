# Makefile for paste (amiport)
include ../common.mk

TARGET      = paste
SOURCES     = ported/paste.c
VERSION     = 1.27
REVISION    = 1
DESCRIPTION = Merge lines of files side by side
AUTHOR      = Adam S. Moskowitz, UC Berkeley

AMINET_CAT  = util/cli

.PHONY: all clean test package compare

test: $(TARGET)
	@echo "=== Testing $(TARGET) ==="
	@echo "=== $(TARGET) tests complete ==="
