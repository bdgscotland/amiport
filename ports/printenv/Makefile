# Makefile for printenv (amiport)
include ../common.mk

TARGET      = printenv
SOURCES     = ported/printenv.c
VERSION     = 1.8
REVISION    = 1
DESCRIPTION = Print environment variables
AUTHOR      = University of California, Berkeley

AMINET_CAT  = util/cli

.PHONY: all clean test package compare

test: $(TARGET)
	@echo "=== Testing $(TARGET) ==="
	@echo "=== $(TARGET) tests complete ==="
