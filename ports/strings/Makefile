# Makefile for strings (amiport)

TARGET      = strings
SOURCES     = ported/strings.c
VERSION     = 1.0
REVISION    = 1
DESCRIPTION = Find printable strings in files
AUTHOR      = amiport

include ../common.mk

.PHONY: all clean test package compare

test: $(TARGET)
	@echo "=== Testing $(TARGET) ==="
	@echo "=== $(TARGET) tests complete ==="
