# Makefile for mv (amiport)

TARGET      = mv
SOURCES     = ported/mv.c
VERSION     = 1.47
REVISION    = 1
DESCRIPTION = Move or rename files
AUTHOR      = Ken Smith, UC Berkeley

include ../common.mk

.PHONY: all clean test package compare

test: $(TARGET)
	@echo "=== Testing $(TARGET) ==="
	@echo "=== $(TARGET) tests complete ==="
