# FS-UAE Test Report: head

## Summary

| Field | Value |
|-------|-------|
| Port | head |
| Date | 2026-03-22 02:26:12 |
| Duration | 60s |
| Platform | FS-UAE 3.2.35 (A1200, Kickstart 3.1) |
| Binary | `WORK:head` (37K) |
| Test method | ARexx harness → TAP output |
| Result | **PASS** — 21/21 passed |

## Test Results

```
1..21
ok 1 - Default 10-line output with no flags
ok 2 - -n flag limits output to specified count
ok 3 - -n 1 prints exactly one line
ok 4 - Legacy -count syntax (-3) limits output
ok 5 - Legacy -1 syntax prints exactly one line
ok 6 - Multiple files produce ==> header lines
ok 7 - -n with value larger than file line count prints whole file
ok 8 - Stdin fallback when no file argument given
ok 9 - Nonexistent file returns error (stderr not captured by ARexx)
ok 10 - Invalid flag returns error (usage goes to stderr)
ok 11 - -n with zero is an invalid count
ok 12 - -n with non-numeric argument is an invalid count
ok 13 - Successful read of existing file returns RC 0
ok 14 - File not found returns RC 10 (RETURN_ERROR)
ok 15 - Empty file produces no output and returns RC 0
ok 16 - Long line file handled correctly (char-by-char read)
ok 17 - File with fewer lines than default 10 prints all lines
ok 18 - -n equal to exact line count of file prints all lines
ok 19 - WORK: volume path resolves correctly for single file
ok 20 - Multiple WORK: paths in single invocation both resolve
ok 21 - One valid and one missing file -- partial error (RC 10 from missing)
# passed: 21 failed: 0 total: 21
```

### Breakdown

| # | Test | Status | Details |
|---|------|--------|---------|
| 1 | Default 10-line output with no flags | PASS | |
| 2 | -n flag limits output to specified count | PASS | |
| 3 | -n 1 prints exactly one line | PASS | |
| 4 | Legacy -count syntax (-3) limits output | PASS | |
| 5 | Legacy -1 syntax prints exactly one line | PASS | |
| 6 | Multiple files produce ==> header lines | PASS | |
| 7 | -n with value larger than file line count prints whole file | PASS | |
| 8 | Stdin fallback when no file argument given | PASS | |
| 9 | Nonexistent file returns error (stderr not captured by ARexx) | PASS | |
| 10 | Invalid flag returns error (usage goes to stderr) | PASS | |
| 11 | -n with zero is an invalid count | PASS | |
| 12 | -n with non-numeric argument is an invalid count | PASS | |
| 13 | Successful read of existing file returns RC 0 | PASS | |
| 14 | File not found returns RC 10 (RETURN_ERROR) | PASS | |
| 15 | Empty file produces no output and returns RC 0 | PASS | |
| 16 | Long line file handled correctly (char-by-char read) | PASS | |
| 17 | File with fewer lines than default 10 prints all lines | PASS | |
| 18 | -n equal to exact line count of file prints all lines | PASS | |
| 19 | WORK: volume path resolves correctly for single file | PASS | |
| 20 | Multiple WORK: paths in single invocation both resolve | PASS | |
| 21 | One valid and one missing file -- partial error (RC 10 from missing) | PASS | |

## Environment

| Component | Version/Path |
|-----------|-------------|
| FS-UAE | 3.2.35 |
| Kickstart | 3.1 (40.68) |
| Amiga model | A1200 (68020) |
| Compiler | m68k-amigaos-gcc (bebbo) |
| POSIX shim | libamiport.a |
| Regex emu | libamiport-emu.a |
| Test harness | ARexx (test-runner.rexx) |

## Test Cases

Each test runs the command inside AmigaOS, captures stdout to a file,
and compares against the expected output string.

```
# test-fsemu-cases.txt -- FS-UAE test suite for head v1.24 (OpenBSD)
# Category 1 (CLI tool) -- minimum 8 tests required
# Generated by test-designer agent

# ---------------------------------------------------------------------------
# 1. FUNCTIONAL TESTS -- one per documented flag/mode
# ---------------------------------------------------------------------------

TEST: Default 10-line output with no flags
CMD: WORK:head WORK:test-head-5lines.txt
EXPECT: alpha
EXPECT_RC: 0

TEST: -n flag limits output to specified count
CMD: WORK:head -n 3 WORK:test-head-5lines.txt
EXPECT: alpha
EXPECT_RC: 0

TEST: -n 1 prints exactly one line
CMD: WORK:head -n 1 WORK:test-head-3lines.txt
EXPECT: first line
EXPECT_RC: 0

TEST: Legacy -count syntax (-3) limits output
CMD: WORK:head -3 WORK:test-head-5lines.txt
EXPECT: alpha
EXPECT_RC: 0

TEST: Legacy -1 syntax prints exactly one line
CMD: WORK:head -1 WORK:test-head-3lines.txt
EXPECT: first line
EXPECT_RC: 0

TEST: Multiple files produce ==> header lines
CMD: WORK:head -n 1 WORK:test-head-3lines.txt WORK:test-head-5lines.txt
EXPECT: ==> WORK:test-head-3lines.txt <==
EXPECT_RC: 0

TEST: -n with value larger than file line count prints whole file
CMD: WORK:head -n 100 WORK:test-head-3lines.txt
EXPECT: first line
EXPECT_RC: 0

# Uses input file instead of piping (ARexx limitation)
TEST: Stdin fallback when no file argument given
CMD: WORK:head -n 2 WORK:test-head-stdin.txt
EXPECT: line one
EXPECT_RC: 0

# ---------------------------------------------------------------------------
# 2. ERROR PATH TESTS
# ---------------------------------------------------------------------------

TEST: Nonexistent file returns error (stderr not captured by ARexx)
CMD: WORK:head WORK:no-such-file-xyz.txt
EXPECT_RC: 10

TEST: Invalid flag returns error (usage goes to stderr)
CMD: WORK:head -z WORK:test-head-3lines.txt
EXPECT_RC: 10

TEST: -n with zero is an invalid count
CMD: WORK:head -n 0 WORK:test-head-3lines.txt
EXPECT_RC: 10

TEST: -n with non-numeric argument is an invalid count
CMD: WORK:head -n abc WORK:test-head-3lines.txt
EXPECT_RC: 10

# ---------------------------------------------------------------------------
# 3. EXIT CODE TESTS
# ---------------------------------------------------------------------------

TEST: Successful read of existing file returns RC 0
CMD: WORK:head -n 1 WORK:test-head-3lines.txt
EXPECT: first line
EXPECT_RC: 0

TEST: File not found returns RC 10 (RETURN_ERROR)
CMD: WORK:head WORK:nonexistent-head-test.txt
EXPECT_RC: 10

# ---------------------------------------------------------------------------
# 4. EDGE CASE TESTS
# ---------------------------------------------------------------------------

TEST: Empty file produces no output and returns RC 0
CMD: WORK:head WORK:test-empty.txt
EXPECT:
EXPECT_RC: 0

TEST: Long line file handled correctly (char-by-char read)
CMD: WORK:head -n 1 WORK:test-longline.txt
EXPECT_CONTAINS: MARKER
EXPECT_RC: 0

TEST: File with fewer lines than default 10 prints all lines
CMD: WORK:head WORK:test-head-3lines.txt
EXPECT: first line
EXPECT_RC: 0

TEST: -n equal to exact line count of file prints all lines
CMD: WORK:head -n 3 WORK:test-head-3lines.txt
EXPECT: first line
EXPECT_RC: 0

# ---------------------------------------------------------------------------
# 5. AMIGA-SPECIFIC TESTS
# ---------------------------------------------------------------------------

TEST: WORK: volume path resolves correctly for single file
CMD: WORK:head -n 1 WORK:test-head-5lines.txt
EXPECT: alpha
EXPECT_RC: 0

TEST: Multiple WORK: paths in single invocation both resolve
CMD: WORK:head -n 1 WORK:test-head-5lines.txt WORK:test-head-3lines.txt
EXPECT: ==> WORK:test-head-5lines.txt <==
EXPECT_RC: 0

TEST: One valid and one missing file -- partial error (RC 10 from missing)
CMD: WORK:head -n 1 WORK:test-head-3lines.txt WORK:no-such-file-xyz.txt
EXPECT_CONTAINS: first line
EXPECT_RC: 10
```

## Emulator Log

```
(log not captured in this run)
```

## Sentinel File

Written by the ARexx harness when all tests complete:

```
TESTS_COMPLETE
passed=21
failed=0
total=21
```

---
Generated by `make test-fsemu TARGET=ports/head`
Report template: `toolchain/templates/test-report.md.template`
