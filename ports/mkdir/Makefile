# Makefile for mkdir (amiport)
include ../common.mk

TARGET      = mkdir
SOURCES     = ported/mkdir.c
VERSION     = 1.31
REVISION    = 1
DESCRIPTION = Make directories
AUTHOR      = University of California, Berkeley

AMINET_CAT  = util/dir

.PHONY: all clean test package compare

test: $(TARGET)
	@echo "=== Testing $(TARGET) ==="
	@echo "=== $(TARGET) tests complete ==="
